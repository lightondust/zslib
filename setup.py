from setuptools import setup, find_packages

setup(name="zslib",
      version="0.1",
      description="",
      url="https://github.com/lightondust/zslib.git",
      packages=find_packages()
      )
